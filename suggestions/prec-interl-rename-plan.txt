Prec / Prec0 identifier migration plan
Date: 2026-09-18
Tracking: GitHub issues #530 and #850

Inventory

The exhaustive machine-readable ledger is:

  suggestions/prec-interl-rename-inventory.tsv

It contains every ASCII Lean identifier or source word containing Prec, Prec0,
prec, or prec0 in RealRooted.lean and RealRooted/**/*.lean.  The audited scan
contains 1,534 unique identifiers, 7,005 occurrences, and 429 files.

Each row records an old identifier, proposed identifier, occurrence count,
file count, category, migration action, and top-level source clusters.

Disposition counts

  393  Lean declarations requiring semantic review
  203  tactic declarations requiring an alias-first migration
  266  tactic references migrated with their owning tactic
   93  local hypotheses reserved for a later style-only pass
   20  statement scaffolds requiring proof-status-preserving renames
  478  deprecated compatibility identifiers with canonical targets
   57  other references, locals, or private declarations migrated directly
   24  lexical false positives kept unchanged

Naming rules

  Prec0 / prec0        -> Interl / interl
  Prec / prec          -> StrictInterl / strictInterl
  prec0_or_revPrec0    -> interl_or_reverse
  prec_or_revPrec      -> strictInterl_or_reverse
  standalone revPrec0  -> reverseInterl
  standalone revPrec   -> reverseStrictInterl
  StrictPrecSameDegree -> StrictInterlSameDegree
  SourcePrec           -> SourceInterl
  rr_prec0             -> rr_interl
  rr_prec              -> rr_strict_interl
  rr_base_prec         -> rr_base_strict_interl

`StrictInterlSameDegree` is a deliberate new name for the distinct strong
same-degree root-interleaving predicate, not an alias for `StrictInterl` plus a
degree equation.  Likewise, `SourceInterl` remains the source-faithful
Hoster--Stump relation with its zero and low-degree cases; it is not collapsed
into the project-wide `Interl` relation.  Reverse names describe argument
orientation in theorem names and do not introduce new reverse predicates.

Compound `strictPrec` / `strict_prec` names belonging to the strong
same-degree predicate become `strictInterlSameDegree` /
`strict_interl_same_degree`; they never acquire a second `strict`.  Names with
`NonnegPrec` are decided from the declaration's result type.  In particular,
the two Garloff--Wagner Hadamard declarations concluding `Interl` become
`garloffWagnerHadamardNonnegInterl_of_oddEven` and
`gwHadamardProductNonnegInterl`.

The TSV applies exact existing canonical targets to deprecated wrappers.  It
keeps English or unrelated lexical matches unchanged, including deprecated,
IsPreconnected, preconnected, preceding, precise, precision, precomposition,
and precursor families.

Two inventory-level proposed-name collisions are intentional.  The deprecated wrappers
strictPrecSameDegree_toPrec and strictPrecSameDegree_toStrictInterl both
target strictInterlSameDegree_toStrictInterl.  Both wrappers keep their old
names and signatures; only the canonical declaration uses the new name.  The
deprecated SumCompatibleLeft.prec_sum and global
prec_sum_of_compatible_left likewise both target
SumCompatibleLeft.toStrictInterl.  All wrappers are removed only after the
deprecation window.

The weighted-sum owner is also canonicalized: `WeightedCompatibleLeft.prec`
and the global `prec_weightedSum_left` compatibility alias both delegate to
`WeightedCompatibleLeft.toStrictInterl`; `prec_sum_left`,
`prec_weightedSum_right`, and `prec_sum_right` now target
`WeightedCompatibleLeft.toStrictInterl_sum`,
`StrictInterl.weightedSum_right_of_nonneg`, and `StrictInterl.sum_right`.
The short token `prec` remains an aggregate inventory row because unrelated
namespaces still have live declarations with that exact identifier.

The generic `PosCombo` combination layer is canonicalized under
`StrictInterl`: nonnegative left/right combinations, their two real-rootedness
consequences, and the three convex-combination endpoints retain exact
deprecated aliases.  `PosComboRealRooted.of_prec` now delegates to
`PosComboRealRooted.of_strictInterl`; the short `of_prec` token remains an
aggregate inventory row because other namespaces still own live declarations
with that name.

The remaining foundational `PosCombo` cone layer is likewise canonicalized:
the same-degree `X - C r` transports are receiver methods on `StrictInterl`,
and the weighted/list common-left cone theorems live in the `StrictInterl`
namespace.  All seven old public signatures remain as deprecated wrappers or
aliases.

The two degree-zero boundary constructors now live under `StrictInterl` as
`of_degree_zero_degree_zero` and `of_degree_zero_right_of_degree_one`.  The
derivative statement interface is `derivativePreservesInterlStatement`; its
checked witness is `derivativePreservesInterl`.  The same-degree reduction,
degree-at-least-two statement interfaces, branch-selection helpers, and direct
applied derivative APIs now use `StrictInterl` / `Interl` terminology; every
old public signature remains as a deprecated compatibility declaration, while
the private scalar-transport helper was renamed without a wrapper.
Application-only constructor callers may still use the deprecated constructor
alias until their owning application batch.

The foundational PF/Wagner `X` layer now consistently uses `Interl` and
`StrictInterl` vocabulary for PF self-relations, fixed-side cone addition,
nonnegative combinations, `X`-multiplication, forward and reverse Wagner
transports, and the translated `X - C r` equivalence.  All 18 public old names
remain exact deprecated aliases; every non-challenge theorem caller and tactic
backend quotation uses the canonical API.

The affine-converse and root-sign layer now names its five public
rightmost-factor, affine-family, and same-degree sign constructors with
`StrictInterl`; the successor-degree Obreschkoff regularization helper is
renamed directly because it is private.  The five public old names remain
exact deprecated aliases, and all non-challenge callers use the canonical
surface.

The affine-family layer now consistently names its basic transports,
high/low-degree converse internals, shifted-pair packages, segment/PF/TNN
endpoints, Ma--Wang endpoint-sign constructors, and subtraction-by-`X`
transport with `Interl` / `StrictInterl`.  All 18 public old names remain exact
deprecated aliases; the 12 private helpers were renamed directly, and all
non-challenge callers use the canonical names.

The private Obreschkoff converse implementation now uses `Interl` /
`StrictInterl` throughout its scalar, successor-degree, same-degree,
rightmost-root, and no-common-root helpers.  Eight remaining declarations and
their local callers were renamed directly, and the already-canonical private
scalar helper is reconciled in the ledger; no public aliases are involved.

The Garloff--Wagner Krein data/expansion layer now uses `StrictInterl` for its
root-multiplicity factors, `gwJL` transports, linear-factor bridge, expansion
statement, checked witness, and Theorem 11 endpoint.  All ten public old names
remain exact deprecated aliases or abbreviations, and the statement scaffold
retains its existing assumption-free checked witness.

The Garloff--Wagner iterative layer now uses `StrictInterl` throughout its
derivative shift, factor transports, weighted-expansion reductions, and
Theorem 11 interfaces.  All 19 public old names remain exact deprecated
aliases or abbreviations.  The three renamed statement scaffolds keep their
existing proof status, while production callers use the canonical names.

The generic iterated derivative-shift layer now names all derivative,
common-factor, exact-root, `TDeriv`, and consecutive-iterate conclusions with
`StrictInterl`.  All 13 public old names remain exact deprecated aliases, and
both production and tactic-backend callers use the canonical API.

The checked Garloff--Wagner Theorem 12 package now uses `Interl` /
`StrictInterl` for its Krein-summand receiver methods, derivative relation,
weighted reductions, factorial Schur-product statement and witness, and
Hadamard endpoints.  All 27 old public names remain exact deprecated aliases
or an abbreviation, and non-challenge callers use the canonical API.  The
aggregate `prec` and `prec0` ledger rows remain open because other namespaces
still own declarations with those exact short names.

The Garloff--Wagner Hadamard endpoint now uses `Interl` for both
double-deleted Schur terms, the Krein-summand receiver methods, the two
weighted-expansion assemblers, and the PF/nonnegative two-pair endpoints.  All
ten old public names remain exact deprecated aliases, and the downstream
nonnegative Hadamard theorem uses the canonical endpoint.

The Hadamard consequence layer now uses `Interl` / `StrictInterl` for its
Garloff--Wagner interfaces, PF and real-rootedness consequences, fixed-factor
transports, and reciprocal-cone closure.  All 14 old public names remain exact
deprecated aliases or abbreviations, and the tactic backend uses the canonical
declarations.  The Veronese-section bridge statements remain with their owner
for a separate semantic batch.

The foundational Hermite--Biehler converse, stable-pencil, oriented-pencil,
low-degree, and adjacent Wronskian/Bezoutian APIs now consistently name their
proper-position conclusions with `StrictInterl`.  All 11 old public names
remain exact deprecated aliases, all non-challenge production and tactic
callers use the canonical declarations, and the private degree-one root helper
was renamed directly.

The implementation-facing Obreschkoff regularization endgame now uses
`StrictInterl` / `StrictInterlOrReverse` in its normalized simple-combination,
regularized-orientation, and successor-degree declarations.  These three
`ObreschkoffConverseInternal` declarations and all their internal callers were
renamed directly without expanding the public compatibility surface.

The foundational right-zero `divX` degree-drop layer now consistently uses
`StrictInterl` / `Interl` for reductions, reconstructions, equivalences,
root/splitting projections, and bundled successor-degree data.  All 17 old
public names remain exact deprecated aliases, and every non-challenge caller
uses the canonical declarations.  The legacy module filename remains unchanged
so existing import paths continue to resolve.

The compatibility closure layer now uses `StrictInterl` / `Interl` in its
X-multiplication endpoint and private cut/threshold helpers.  The public
endpoint retains an exact deprecated alias; the five private declarations and
all their callers were renamed directly.

The classical Hurwitz stability layer now uses `StrictInterl` for its parity-
part, rotated-part, coefficient-boundary, and Routh-reduction interfaces.  All
nine old public names, including the `IsStrictlyHurwitzStable` receiver methods,
remain exact deprecated aliases, and every non-challenge caller uses the
canonical declarations.

The foundational affine proper-position helper layer now uses `StrictInterl` /
`Interl` for constant, affine-linear, determinant, congruence, and translated
linear-pair lemmas.  All nine old public names remain exact deprecated aliases,
and callers across compatibility, threshold, matrix, row-threshold, and
Veronese modules use the canonical declarations.

The magnitude-dominated and upper-derivative criterion layer now uses
`StrictInterl` for its same-degree, successor-degree, combined, automatic, and
derivative-evaluation interfaces.  All five old public names remain exact
deprecated aliases, and both production and tactic-backend callers use the
canonical declarations.

The Wagner right-sum sign layer now names both boundary evaluation lemmas with
`StrictInterl`; their old public names remain exact deprecated aliases.

The two private affine Favard step/sequence helpers now use `StrictInterl`
terminology and were renamed directly with their callers.

The shift-lemma layer now uses `StrictInterl` for its differ-by-one,
same-degree, general, and application-variable interfaces.  All four public
old names (including the primed wrapper represented by the aggregate ledger
token) remain exact deprecated aliases.

The matrix interlacing pair layer now uses `StrictInterl` / `Interl` for its
2-by-2 zip/sum, add/multiply, weak, and sparse converse interfaces.  All five
old public names remain exact deprecated aliases, and non-challenge callers use
the canonical declarations.

The gamma-pencil symbol and all-rank theorem layers now use `StrictInterl` /
`Interl` for their residual-factor, operator-orientation, and component-pair
interfaces.  All four old public names remain exact deprecated aliases, and
the stable-symbol callers use the canonical declarations.

The foundational sequence-closure layer now uses `StrictInterl` terminology
for chain induction, degree-branch dispatch, real-rootedness projections, and
left/right pair projections.  All 18 old public signatures remain as
deprecated compatibility aliases, while non-application callers and tactic
backend quotations use the canonical declarations.

The two foundational directions of the Obreschkoff all-combinations bridge
are now `allComboRealRooted_of_strictInterl` and
`strictInterl_of_allComboRealRooted`.  Their old public names remain exact
deprecated aliases, and non-challenge callers use the canonical declarations.

The strict-sign Ma--Wang constructor layer now consistently names its
`StrictInterl` conclusions: the raw same/successor root-sign constructors,
endpoint transport, three structured coefficient-sign constructors, and the
strict-step simple-root package all use `strictInterl` terminology.  Their old
public names remain exact deprecated aliases, and non-challenge callers use
the canonical declarations.

The weak-sign Ma--Wang constructor layer now likewise uses `StrictInterl`
terminology for its generic and structured same/successor/degree-bounded
theorems and for the regularized `sub_C_mul` transport.  Exact deprecated
aliases preserve the old API, while theorem-library and tactic callers use the
canonical declarations.

The foundational CommonInterleaver pair bridge now names forward, reverse,
and disjunctive orientations with `strictInterl` / `reverseStrictInterl`.
This includes the all-combinations and compatibility endpoints, both common
interleaver directions, and the nonnegative right-pair boundary constructor.
All nine old public names remain exact deprecated aliases.

Execution order

1. Rename vocabulary-defining predicates and structures.  The
   StrictPrecSameDegree family is now canonicalized as StrictInterlSameDegree,
   and the source-faithful Hoster--Stump relation is now canonicalized from
   SourcePrec to SourceInterl.  Both retain deprecated compatibility APIs; the
   strict same-degree tactics also retain parser-compatible wrappers.  Continue
   with relation-bearing Statement scaffolds, preserving proof-status language
   and compatibility aliases where public.

2. Migrate generic foundational declaration families in bounded modules:
   WagnerX affine/common-factor transports, nonnegative-root X transports,
   Wagner left/right sums, weighted sums, and the residue/outer-sign layer are
   now canonical receiver or relation-namespace APIs.  Continue with remaining
   generic StrictInterl/Interl receiver methods.  Keep deprecated wrappers with
   exact old signatures.

3. Migrate concentrated zero-aware domain APIs by owner:
   BrandenLeite, BrandenVecchi, PF-polynomial, matrix, and sequence families.

4. Migrate specialized theorem families by module:
   Jacobi, Ma-Wang, Liu-Wang, Garloff-Wagner, CommonInterleaver, parking
   functions, classical Hurwitz, symmetric decomposition, Veronese, and OEIS
   applications.  Do not infer orientation from spelling alone.

5. Add tactic aliases before changing tactic clients:
   rr_interl, rr_strict_interl, and rr_base_strict_interl first; then quoted
   declarations, elaborator patterns, syntax fields, generated names, examples,
   and coverage tables in the same verified batch.

6. Perform local-variable and prose cleanup only after API owners migrate.
   Names such as hprec are changed according to their actual type, not by a
   repository-wide textual rule.

7. Remove deprecated wrappers only in a later compatibility-removal issue.

Replacement protocol

The #893 core-orientation batch canonicalizes `Compatible.of_strictInterl` and
the four public PosComboRealRooted forward-or-reverse/root-asymmetry selectors.
Exact deprecated aliases preserve every former public namespace and signature;
all non-challenge callers use the explicit StrictInterl vocabulary.

The #894 CommonInterleaver slot/root-sequence batch canonicalizes the two
descending-root slot constructors, the two root-slot membership projections,
the checked left shifted-slot statement and witness, the root-sequence
characterization, and the endpoint-closed consecutive-chain theorem.  Exact
deprecated declarations preserve all nine former public names.  The renamed
shifted-slot proposition retains its existing checked witness and unchanged
proof status.

The #896 descending-root facade batch canonicalizes the three public
`CommonInterleaverSeq` wrappers and their private `List.ofFn` helper.  Exact
deprecated aliases preserve the public signatures; the private helper is
renamed directly.

The #897 succ-degree root-count batch canonicalizes the eleven public
residual/`divX` root-count, root-crossing, and pair-interleaver bridges, plus
the private root-slot witness.  Exact deprecated aliases preserve the public
APIs; theorem-library and tactic backends now quote canonical declarations,
while tactic syntax remains available for its later alias-first migration.

The #898 foundational orientation-tail batch canonicalizes the two public
forward one-two-family sum transports and directly renames the private
Obreschkoff same-degree and affine right-pair orientation helpers.  Exact
deprecated aliases preserve the two public CommonInterleaver signatures.

The #899 Branden--Leite chain-polynomial batch canonicalizes the zero-aware
`fPolynomial` transport and the weak/strict consecutive-chain theorems,
including their totally-nonnegative specializations.  Exact deprecated aliases
preserve all five public signatures.

The #900 shared transport batch canonicalizes the zero-aware `mul_X` theorem,
the PF coefficient-limit theorem, and the full checked
`strictInterlFPolynomialTransport` statement/minimal/witness family together
with its Branden--Solus conditional consumer.  Exact deprecated compatibility
declarations preserve all eight public names; proof status is unchanged.

The #901 Branden--Leite composition-chain batch canonicalizes the seven weak
interlacing packages for constant-diagonal chains, finite lower-triangular
chains, positive-part/zero-constant composition rows, and inverse-power rows.
Exact deprecated aliases preserve every public signature.

The #902 Branden--Leite kernel/tiling batch canonicalizes the eight weak
interlacing packages for finite and limiting kernel rows, two-kernel rows,
weighted shift tilings, and the weighted-green/marked-shift specialization.
Exact deprecated aliases preserve every public signature.

The #903 final Branden--Leite package batch canonicalizes the ten remaining
binomial, monomer, rational, shifted, and OEIS-labeled PF/interlacing package
names.  Exact deprecated aliases preserve every public signature; no
Branden--Leite-owned semantic declaration row remains in the ledger.

The #892 compatibility/right-pencil batch canonicalizes the failed succ-degree
orientation statement interface and its seven checked conditional reductions.
The statement remains an unproved target; its deprecated abbreviation and the
seven exact theorem aliases preserve the old API without changing that boundary.

The #891 root-count batch canonicalizes the five foundational same- and
succ-degree theorems that turn StrictInterl geometry into oriented root-count
inequalities.  Exact deprecated aliases preserve callers outside the migrated
CommonInterleaver and LiuOppositeSigns theorem layer.

The #890 succ-degree endpoint batch canonicalizes two explicitly unproved
statement scaffolds and three checked StrictInterl/divX bridge declarations.
Deprecated abbreviations and aliases preserve the former public API; production
and tactic callers use the canonical names, while regression/example callers
remain on compatibility names for a later example cleanup.

The #889 CommonInterleaver orientation/boundary batch canonicalizes the seven
tightly coupled low-degree orientation, ordered all-combinations, affine-family
right-pair, and affine-boundary declarations.  Exact deprecated aliases retain
the prior public signatures and namespaces; all non-challenge callers now use
the explicit StrictInterl-based names.  The root-count/scaffold layer remains a
separate dependency-ordered batch.

- Use the TSV as an exact whole-identifier map; never replace substrings.
- For rows marked manual_semantic_rename, inspect the theorem result and
  argument orientation before editing.  Prefer namespaced receiver APIs where
  the relation witness is a natural receiver.
- For statement scaffolds, rename both statement and witness references without
  changing whether the target is proved, conditional, or still open.
- For every batch, migrate declarations and all owned callers together, retain
  compatibility wrappers where required, then run focused, affected,
  Production, full-root, source-guard, residual-name, and axiom checks.
- Keep local Lake builds serialized.

Validation of this ledger

- The TSV has seven fields on every row.
- Every non-false-positive proposal is free of Prec/Prec0/prec/prec0 fragments.
- No accidental proposed-name collisions remain beyond the documented
  deprecated-wrapper convergence.
- The 24 false-positive rows map exactly to themselves.
